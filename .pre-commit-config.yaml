repos:
  - repo: https://github.com/psf/black
    rev: 23.9.1  # Use the latest stable version of Black
    hooks:
      - id: black
        args: [--line-length=120]  # Optional: Adjust line length as needed
